# Plan: デプロイ基盤（stg/prod compose + nginx + CD + prod provisioning）

| 項目 | 値 |
|---|---|
| ステータス | 🟣 マージ承認待ち（再コードレビュー APPROVE・PR 作成→笹木さんマージ承認待ち） |
| slug | `deploy-infra` |
| 作成 | 2026-06-12 15:30 JST |
| 関連 PR / レビュー | 親 Plan の [マイルストーン計画レビュー(Phase 6)](../reviews/2026-06-12-1500-pleasanter-exit-1on1-rebuild-review.md) を起点に分離 / [計画レビュー](../reviews/2026-06-12-1545-deploy-infra-review.md): NEEDS WORK / [再計画レビュー v2](../reviews/2026-06-12-1600-deploy-infra-review-v2.md): APPROVE / [コードレビュー](../reviews/2026-06-12-1648-deploy-infra-code-review.md): BLOCKED / [コードレビュー v2](../reviews/2026-06-12-1715-deploy-infra-code-review-v2.md): APPROVE |
| 親 Plan | [pleasanter-exit-1on1-rebuild](2026-06-11-1730-pleasanter-exit-1on1-rebuild.md)（Phase 0–5 実装済み・CI ✅。本 Plan は Phase 6 の「デプロイ基盤」を独立化したもの） |
| 一次情報 | [infra/docker-compose.yml](../../infra/docker-compose.yml) / [infra/.env.example](../../infra/.env.example) / [seed/10_users.sql](../../outputs/infra-data/seed/10_users.sql) / [.github/workflows/](../../.github/workflows/) |

> 親 Plan の Phase 6 が「3 行」で、デプロイの 6 観点（B-1〜B-6）が未決だったため独立 Plan 化（計画レビュー指摘）。
> 本 Plan は **stg/prod を安全に建てる**ことだけにスコープを絞る。CI（GitHub Actions）は親 Plan で完了済み。

---

## 1. ゴール / 非ゴール

### ゴール

waoon を **stg / prod で安全に起動・更新**できる基盤を作る。

- `apps/web` の **本番 Dockerfile** + **stg/prod の docker-compose**（dev と同型 + 本番差分）。
- **nginx**（TLS 終端・ルーティング・X-Forwarded-For 注入、必要ならエッジ rate-limit）。
- **secrets 管理**: dev フォールバック（`dev-only-change-me…`）が **prod で絶対に使われない**保証。
- **CD**: GitHub Actions で build → （registry）→ host 反映 → `db:migrate` → 起動 の一連を定義。
- **prod 初期化**: 組織マスタ seed のみ + **admin 1 名を GoTrue 経由で provisioning**（手書き UUID seed は使わない）。
- **DB バックアップ/リストア**方針。

### 非ゴール

| 項目 | 理由 |
|---|---|
| 実データ移行（Pleasanter→Postgres） | 親 Plan の非ゴールのまま。初期状態は「空 + admin 1 名」 |
| 初回ログイン後の force-change / メール recover の本実装 | SMTP 方針（§4）次第。導入するなら別タスク |
| Redis 等の外部レートリミットストア | まず nginx で足りる。多重インスタンス化したら検討 |
| 監視/メトリクス/ログ集約・オートスケール | MVP のデプロイ後に別途 |
| ステージング以外の複数環境（プレビュー環境等） | 後 |

---

## 2. スコープ境界

- **触る**: `infra/Dockerfile.web`（新規）/ `infra/docker-compose.stg.yml` / `infra/docker-compose.prod.yml` / `infra/nginx/`（新規）/ `infra/.env.stg.example` `.env.prod.example` / `.github/workflows/`（CD 追加）/ `scripts/provision.mjs`（新規）/ `scripts/db-migrate.mjs`（`COMPOSE_FILE` / `PG_SERVICE` 対応に改修）/ `.gitignore`（`!.env.*.example` 例外追加）/ ルート `package.json` scripts（`compose:stg/prod` 等）。
- **追加（コードレビュー B-1 反映）**: `infra/data/05_rotate_roles.sh`（stg/prod の initdb で内部ロール PW を env へ rotate）/ `scripts/lib/env.mjs`（env パーサ共有）。
- **触らない**: 業務コード（`apps/web` のアプリ実装）・`00_bootstrap.sql` 本体の DDL（dev 用 role 作成はそのまま。stg/prod は別 initdb スクリプトで PW のみ rotate）・dev の `infra/docker-compose.yml`（dev は現状維持）。
- **前提（所与）**: Docker Compose / GitHub Actions / GoTrue `supabase/auth:v2.189.0` / postgres:15 自前 image。デプロイ先は §3 の「要決定」を確定してから。

---

## 3. 要決定（計画レビューで詰めたい論点）

実装着手前に確定が要る。**推奨案**を併記するが、最終決定は笹木さん。

| # | 論点 | 選択肢 | 推奨（暫定） |
|---|---|---|---|
| D-1 | デプロイ先ホスト | 既存 stg server(SSH/Docker) / VPS / マネージド | 技術メモの **stg server（Linux + Docker）** 前提で進める |
| D-2 | CD 方式 | push 型（GHCR に image → host pull）/ pull 型（host で git pull + build） | **push 型（GHCR）**。host は compose pull + up のみで軽い |
| D-3 | TLS 証明書 | certbot(Let's Encrypt) / 持ち込み証明書 | **certbot**（自動更新）。ドメインがある前提 |
| D-4 | レートリミット責務 | nginx 一次に移譲 / API 層と多層 | **多層**（nginx `limit_req` を一次、API 層 limiter は残す）。nginx が XFF 注入、API は実 IP を読む |
| D-5 | GoTrue SMTP | 入れる（recover/force-change 可能に）/ 入れない（admin 再発行のみ） | **当面入れない**（admin がパスワード再発行→別経路で通知）。SMTP は後続。`MAILER_AUTOCONFIRM=true` は維持 |
| D-6 | DB バックアップ | host cron `pg_dump` / pg_cron / マネージド | **host cron で日次 `pg_dump`** → host 外ストレージ。まず最小 |

---

## 4. 現状コンテキスト（差分の起点）

- **dev compose**: postgres + gotrue のみ。**web service・nginx は無い**（dev は `pnpm dev` 直起動）。
- **secrets フォールバック**: compose は `${JWT_SECRET:-dev-only-change-me-please-32bytes-minimum}` 等、**全 secret に dev default**。`AUTH_ADMIN_PASSWORD` `PG_SUPERUSER_PASSWORD` も同様。→ prod で env を渡し忘れても黙って dev secret で起動する穴（**B-2**）。
- **GoTrue**: `GOTRUE_DISABLE_SIGNUP=true` / `MAILER_AUTOCONFIRM=true` / SMTP なし / `SITE_URL`・`API_EXTERNAL_URL` が localhost 既定。
- **seed**: `10_users.sql` が GoTrue UUID 手書き（dev で作った id）。**prod では対応 identity が無く使えない**（**B-6**）。`scripts/provision.mjs` は未存在。
- **provisioning 実装は既にある**: `POST /api/v1/users` が GoTrue admin API で identity 発行 + `gotrue_id` 紐付け + 初期 PW 生成（#19）。prod 初期 admin は **これと同じ経路をスクリプト化**すればよい。
- **CI**: `.github/workflows/` で typecheck/build/pgTAP（#10, ✅）。CD は未。

---

## 5. 実装ステップ（順序付き）

> B-2（secrets 遮断）と B-6（prod admin provisioning）を最優先（事故・ログイン不能直結）。

### Step 1 — secrets 遮断 + env テンプレート（B-2）⟦最優先⟧
1. `docker-compose.prod.yml` では secrets の **default フォールバックを外す**（`${JWT_SECRET:?JWT_SECRET required}` 形式）。未設定なら起動失敗。
2. `infra/.env.stg.example` / `infra/.env.prod.example` を用意（必須キー列挙、値は空 or プレースホルダ）。実 `.env.*` は **リポジトリに入れない**。
   - **`.gitignore` 例外を追加する**（N-1）。現行 [`.gitignore`](../../.gitignore#L10-L13) は `.env.*` を無視し `!.env.example` だけ例外にしているため、`*.example` テンプレートが **コミットされず取りこぼす**。`.env.*` の直後に `!.env.*.example` を 1 行足して example 系を一括で追跡対象に戻す（`infra/` 配下にもパターンが効くことを確認する）。
   - 【検証】`git check-ignore infra/.env.stg.example infra/.env.prod.example` が **何も出力しない**（= 追跡対象）こと、`git status` に両 example が現れること。
3. **`dev-only-change-me` が prod に出ていない**ことの起動時 or CI チェック（grep ガード）を 1 つ入れる。
- 【検証】env 未設定で `compose -f prod up` が **起動失敗**すること。

### Step 2 — web の本番 image + stg/prod compose（B-1, N-2）
1. `infra/Dockerfile.web`（Next.js standalone build, `pnpm --filter @waoon/web build` → runner）。
2. `docker-compose.stg.yml` / `docker-compose.prod.yml`（postgres + gotrue + **web** + **nginx**、stg/prod 同一構成、env で差分）。dev は web を載せない非対称を明記（N-2）。
3. ルート `package.json` に `compose:stg:*` / `compose:prod:*`（親 Plan 例の命名に合わせる）。
- 【検証】stg compose で web まで起動し、nginx 経由でトップが描画。

### Step 3 — nginx（B-3, D-3, D-4）
1. `infra/nginx/`（`server_name`、TLS 終端=certbot、web への upstream、`proxy_set_header X-Forwarded-For` で**信頼できる実 IP を注入**）。
2. `limit_req`（zone）を一次レートリミットに。API 層 limiter は多層防御として残す（判断ログ D-4）。
- 【検証】HTTPS で到達 / `/api/v1/auth/login` を叩いて nginx 経由でも 429 が出る / API が XFF の実 IP を見ている。

### Step 4 — GoTrue prod 設定（B-4, D-5）
1. `SITE_URL` / `API_EXTERNAL_URL` を prod ドメインへ。`DISABLE_SIGNUP=true` 維持。`MAILER_AUTOCONFIRM=true` 維持（SMTP 無し方針）。
2. SMTP を入れない判断を判断ログに固定（recover は admin 再発行のみ・force-change は後続）。
- 【検証】prod GoTrue でログイン/refresh が通る。

### Step 5 — CD パイプライン + migration 運用（B-1, B-5, D-2）
1. GitHub Actions: main/develop の所定トリガで `Dockerfile.web` を build → **GHCR push**。
2. **migration runner を確定する**（B-5）。現行 [`scripts/db-migrate.mjs`](../../scripts/db-migrate.mjs#L9-L11) は compose file を `infra/docker-compose.yml`（dev）に **固定参照**しており、このままでは stg/prod compose に対して走らない。次の方式で stg/prod に対応させる:
   - **`db-migrate.mjs` を `COMPOSE_FILE` 対応にする**（推奨）。`const composeFile = process.env.COMPOSE_FILE ?? join(root, "infra", "docker-compose.yml")` とし、対象 service 名も `PG_SERVICE`（既定 `postgres`）で上書き可能にする。CD は `COMPOSE_FILE=infra/docker-compose.prod.yml PG_SERVICE=postgres pnpm db:migrate` の形で呼ぶ。dev は env 未設定で従来どおり動く（後方互換不要だが既定値で自然に温存される）。
   - 代替案: `docker-compose.{stg,prod}.yml` に **ワンショット `migrate` service**（`profiles: [migrate]`, web と同 image, entrypoint で `psql` 適用）を置き、`docker compose -f infra/docker-compose.prod.yml run --rm migrate` で流す。runner を image 内に閉じ込めたい場合に採用。
   - どちらでも **migration は postgres healthy 後・web 起動前**に走らせる（下記 CD 順序で固定）。
3. host 側 deploy（D-2 の push 型を所与）。CD 順序を以下に固定する:
   `compose pull → postgres/gotrue up → postgres healthcheck 待ち → migration runner（上記）→ web/nginx up`。手動 step か Actions の SSH deploy かの最終形は D-2 に従う。
4. migration は **前進のみ**（down なし）。失敗時は **fix-forward（人手）**と明記。web を **起動させないまま**修正 migration を流し直せる順序にしておく。
- 【検証】stg compose（`COMPOSE_FILE=infra/docker-compose.stg.yml`）に対し `pnpm db:migrate` が schema を適用できる。CD で stg が更新され、migration が **web 起動前**に流れる。

### Step 6 — prod provisioning + バックアップ（B-6, D-6, N-1）
1. `scripts/provision.mjs`: 組織マスタ seed（手書きユーザー行は除外）+ **admin 1 名を GoTrue admin API で発行 → `users.gotrue_id` 紐付け → 初期 PW を一度だけ表示**（#19 のロジックを CLI 化）。
2. 日次 `pg_dump`（host cron）→ host 外ストレージ。リストア手順（新 DB へ restore → `db:migrate`）を 1 段落で残す。
- 【検証】まっさらな prod で provision 実行 → その admin でログイン成功。`pg_dump` → 別 DB へ restore が通る。

---

## 6. 検証（受け入れ基準）

- env 未設定で prod compose が起動失敗（secrets 遮断）。
- `dev-only-change-me` が prod の起動構成に出ない（grep ガード green）。
- nginx 経由 HTTPS でアプリ到達、ログイン→保護ルート到達。
- CD で stg が更新され、migration が web 起動前に流れる。
- prod を空から `provision.mjs` で初期化 → 発行された admin でログイン成功。
- `pg_dump` バックアップ → 別環境へ restore → 起動成功。

---

## 7. リスクと緩和

| リスク | 影響 | 緩和 |
|---|---|---|
| dev secret が prod に漏れ起動 | 認証バイパス級 | Step 1 のフォールバック撤去 + grep ガード（最優先） |
| prod に手書き UUID seed が流れ、ログイン不能ユーザーが入る | 運用不能 | Step 6 で seed と provisioning を分離・手書きユーザーは prod に流さない |
| migration 失敗で prod 不整合 | データ事故 | 前進のみ + fix-forward 明記 + Step 6 バックアップで復旧可能に |
| XFF 詐称でレートリミット回避 | ブルートフォース | nginx が信頼できる実 IP を注入し API が trust（D-4） |
| TLS 証明書の更新漏れ | 接続不能 | certbot 自動更新（D-3） |

---

## 8. 判断ログ

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-06-12 | Phase 6 の「デプロイ基盤」を親 Plan から **独立 Plan 化** | 計画レビュー(Phase 6 焦点) NEEDS WORK の指摘。6 観点が判断密集で 3 行に収まらない |
| 2026-06-12 | （要決定 D-1〜D-6 は計画レビューで確定予定） | 暫定推奨は §3 に記載。Codex/笹木さんの確定待ち |
| 2026-06-12 | 計画レビュー NEEDS WORK を反映: Step 5 に **migration runner 方式**（`db-migrate.mjs` を `COMPOSE_FILE`/`PG_SERVICE` 対応＋CD 順序固定）、Step 1 に **`.gitignore` 例外**（`!.env.*.example`）を追記 | [計画レビュー](../reviews/2026-06-12-1545-deploy-infra-review.md) の B-1 / N-1。再レビュー依頼へ |
| 2026-06-12 | 再計画レビュー **APPROVE**。D-1〜D-6 暫定推奨を承認扱い・実装は stg first で進める | [再計画レビュー v2](../reviews/2026-06-12-1600-deploy-infra-review-v2.md)。残は笹木さん承認のみ |
| 2026-06-12 | 笹木さん承認 → 実装着手（feature/deploy-infra） | stg first で Step 1〜6 を実装 |
| 2026-06-12 | **D-5: GoTrue SMTP は入れない**を確定。`MAILER_AUTOCONFIRM=true` 維持、recover は admin 再発行のみ | SMTP 基盤が無く、初回 PW は provision/作成 API がサーバ生成して一度だけ表示する運用。force-change は後続タスク |
| 2026-06-12 | nginx は **web のみ public**、GoTrue は内部のみ（公開しない） | `GOTRUE_URL` は server-only でブラウザは GoTrue を直叩きしない。攻撃面を減らすため `/auth/` proxy を置かない |
| 2026-06-12 | XFF は nginx が `$remote_addr` で**上書き**（append しない） | `getClientIp` が XFF 先頭を信頼するため、単一の信頼値に固定してレートリミット回避を封じる（D-4） |
| 2026-06-12 | ~~`app_user`/`supabase_auth_admin` の内部 PW は 00_bootstrap.sql 固定のまま~~（**下記で撤回**） | （当初）postgres を network 非公開にし gate 対象を `JWT_SECRET`/`PG_SUPERUSER_PASSWORD` に絞る判断。→ コードレビュー B-1 で B-2 未達と判定され撤回 |
| 2026-06-12 | provision の GoTrue 呼び出しは compose ネットワーク上の**使い捨て curl コンテナ**経由 | postgres/gotrue を非公開にしたまま host から到達するため。DB 書き込みは `docker compose exec postgres psql` |
| 2026-06-12 | **撤回 → 内部ロール PW も env 由来へ rotate する**（B-1 反映）。`AUTH_ADMIN_PASSWORD`/`APP_DB_PASSWORD` を必須 secret 化し、initdb の `infra/data/05_rotate_roles.sh` で `ALTER ROLE` 適用。`DATABASE_URL` は compose が `APP_DB_PASSWORD` から組み立てる。`check-secrets` は 4 secret 全てを検査 | [コードレビュー](../reviews/2026-06-12-1648-deploy-infra-code-review.md) B-1。dev 固定値(`authadmin`/`app`)が本番に残ると B-2 未達。00_bootstrap.sql 本体（dev 用 DDL）は触らず、stg/prod だけ initdb で rotate（dev は本スクリプトを mount しない） |
| 2026-06-12 | **CD の `dc pull` を `--ignore-buildable` + `dc build postgres` に修正**（B-2 反映）。`db-migrate.mjs` は env file の `PG_DATABASE`/`PG_SUPERUSER` を読む（N-1） | [コードレビュー](../reviews/2026-06-12-1648-deploy-infra-code-review.md) B-2/N-1。buildable な postgres を pull して `set -e` で deploy が止まる事故を回避 |
| 2026-06-12 | Codex トークン切れのため**レビュアー Agent（security/code/architect）を並列起動して再コードレビュー** → **APPROVE**（BLOCKER 0）。安価な NICE-TO-HAVE は即修正、残りは §残課題へ | [コードレビュー v2](../reviews/2026-06-12-1715-deploy-infra-code-review-v2.md)。読み取り専用の 3 視点を統合 |

---

## 9. 残課題（コードレビュー v2 の NICE-TO-HAVE。後続タスク）

実装安全性には影響しない。デプロイ後・別 Plan で拾う。

- nginx に**セキュリティヘッダ**（HSTS / X-Content-Type-Options 等）を付与（未設定）。
- nginx 一次レートリミットを `/api/v1/auth/` 限定から **`/api/` 全体の緩い zone** へ拡張検討。
- `provision.mjs` の service_role JWT を **argv → stdin 経由**に、`aud` を admin 用途に絞る（既存 [`provisioning.ts`](../../apps/web/lib/auth/provisioning.ts) と併せて）。
- `infra/backup/README.md` のリストア例の DB 名/ユーザを **env 由来**に揃える。
- stg/prod の同型 2 compose は現状維持。**環境が 3 つ以上**になったら `include` 化を再検討。
- **PW ローテーション運用**: 既存 volume で `AUTH_ADMIN_PASSWORD`/`APP_DB_PASSWORD` を変える手順は `05_rotate_roles.sh` のコメントに記載済み。正式手順化は後続。

## 10. ステータス

- [x] Plan ドラフト完成（本ファイル）
- [x] **計画レビュー（Codex）** → [NEEDS WORK](../reviews/2026-06-12-1545-deploy-infra-review.md)（migration runner 明確化待ち）
- [x] 指摘対応: Step 5 に stg/prod migration runner 方式を追記、Step 1 に env example の `.gitignore` 例外を追記
- [x] **再計画レビュー（Codex）** → [APPROVE](../reviews/2026-06-12-1600-deploy-infra-review-v2.md)
- [x] 笹木さん承認
- [x] Step 1（secrets 遮断）→ … → Step 6（provisioning/backup）実装（feature/deploy-infra）
- [x] ローカル検証: `pnpm typecheck` green / `docker compose config` 両 compose OK / `check-secrets` がテンプレートを reject
- [x] コードレビュー（Codex）→ [BLOCKED](../reviews/2026-06-12-1648-deploy-infra-code-review.md)
- [x] 指摘対応: DB role secrets（`supabase_auth_admin`/`app_user`）の env 化（initdb rotate + `check-secrets` 4 secret 検査）、CD `dc pull --ignore-buildable`、`db-migrate` の env-file 読み込み（N-1）
- [x] 再検証: `pnpm typecheck` green / 両 compose `config` OK（`DATABASE_URL` が `APP_DB_PASSWORD` から組まれる）/ `check-secrets` が template・dev-leak を reject・実値を pass
- [x] **再コードレビュー** → [APPROVE](../reviews/2026-06-12-1715-deploy-infra-code-review-v2.md)（レビュアー Agent 3 視点・BLOCKER 0。NICE-TO-HAVE は反映 or §9 残課題）
- [ ] PR 作成（develop 向け）→ 笹木さんマージ承認
- [ ] PR merge
- [ ] **マージ後検証**（stg 実環境・笹木さん）
  - [ ] `infra/.env.stg` を実値で用意し `pnpm compose:stg:up` が起動（未設定 secret で起動失敗することも確認）
  - [ ] CD（develop push）で GHCR build/push → stg deploy、migration が web 起動前に流れる
  - [ ] nginx 経由 HTTPS でトップ描画 / `/api/v1/auth/login` が nginx 一次レートリミットで 429
  - [ ] `pnpm provision:prod`（stg 相当）で admin 発行 → そのアカウントでログイン成功
  - [ ] `backup-db.sh` で dump → 別 DB へ restore 成功
