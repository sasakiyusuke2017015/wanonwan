# Plan: ダークモード（semantic トークン反転 + colorScheme 軸）

| 項目 | 値 |
|---|---|
| 概要 | 仕上げ Phase 3 のサブ Plan。反転境界を semantic トークン層に引き、`colorScheme` 軸（light/dark/system）+ FOUC 対策を追加。背景テーマ 9 軸とは直交させ、app の 143 箇所のハードコード色を semantic トークンへ repoint する |
| ステータス | 🟡 実装中 |
| 前提 Plan | [仕上げ（テーマ5）](2026-07-05-2015-finishing-touches.md) / [VRT](2026-08-14-0020-vrt.md) |
| PR | |
| Review | [計画レビュー](../reviews/2026-08-14-0043-dark-mode-review.md) / [コードレビュー 3a-3c](../reviews/2026-08-14-0933-dark-mode-review.md) |

## 目的

アプリは実質ダーク未対応。テーマは `色(6) × 形(3) × 背景(9)` の 3 軸すべてが明色前提で、
色は JS で HSL 生成 → inline style 適用のため CSS 側から反転できない。

**反転境界を semantic トークン層に引く**ことで、catalog 部品とアプリ画面をまとめて反転させ、
ユーザーが light / dark / system を選べる状態にする。

## スコープ

### やること

- semantic トークンの dark 上書き（`[data-theme-mode="dark"]`）
- `body` 地色の semantic 化（現在 `var(--color-white)` 固定で反転しない）
- `colorScheme` 軸（light / dark / system）+ 永続化 + 切替 UI
- FOUC 対策（pre-paint inline script + `suppressHydrationWarning`）
- `design.ts` の inline 生成は**前景/neutral のコントラストのみ** dark 調整（ブランド色相は保持）
- app の **143 箇所 / 33 ファイル**のハードコード色を semantic トークンへ repoint

### やらないこと（スコープ外）

- **色生成ロジック（HSL 動的生成）そのものの作り直し**
- 新しい色テーマの追加・2 テーマ（business/modern）構想
- **WCAG AA 全面準拠**（下記「a11y の目標」参照。今回は目視 + 主要導線のみ確認）
- ダーク撮影の VRT 追加（4-dark）。[VRT Plan](2026-08-14-0020-vrt.md) 完了後の別作業

## 現状コンテキスト（2026-08-14 時点）

| 項目 | 実測値 |
|---|---|
| テーマ軸 | `色(6) × 形(3) × 背景(9)`（[design.ts](../../packages/ui/core/constants/design.ts)）。**colorScheme 軸なし** |
| theme atoms | `colorThemeAtom` / `shapeThemeAtom` / `backgroundThemeAtom`（[atoms.ts](../../packages/ui/infra/theme/atoms.ts)）。dark 系なし |
| 色の適用 | JS で HSL 生成 → **inline style**。CSS 変数でもクラスでもない |
| semantic トークン | [tokens.css](../../packages/ui/core/styles/tokens.css) に `--color-*` が **106 個**。`@theme static` の単一値のみで dark 再定義なし |
| `prefers-color-scheme: dark` | [globals.css](../../packages/ui/core/styles/globals.css) に存在するが、アプリは `tokens` しか import しておらず**デッドコード** |
| `body` 地色 | [apps/web/app/globals.css](../../apps/web/app/globals.css) で `background: var(--color-white, #ffffff)` **固定** |
| ハードコード色 | apps/web の **33 ファイル / 143 箇所**（`text-slate-*` / `bg-white` / `text-gray-*` / `text-red-*` / `border-gray-*` 等） |
| dark 実装 | `data-theme-mode` / `@custom-variant` / `colorScheme` は**ソース全体でゼロヒット** |

## 実装計画

親 Plan の三層モデルに沿い、**小さく landing してから広げる**。

1. **3a. semantic トークンの dark 上書き**（`feature/dark-tokens`・**単独 PR**）
   `tokens.css` の `--color-surface` / `--color-text-*` / `--color-border-*` / `--color-bg-*` を
   `[data-theme-mode="dark"]` で上書き。Tailwind v4 の
   `@custom-variant dark (&:where([data-theme-mode="dark"] *))` を配線する。
   **この PR 単独で、手動で属性を付けたときに catalog（DataTable 等）が反転すること**を検証してから次へ。
2. **3b. `body` 地色の semantic 化**（同 PR または直後）
   `var(--color-white)` → semantic surface トークンへ張替。143 箇所を触る前の土台。
3. **3c. colorScheme 軸 + FOUC 対策**（`feature/dark-toggle`）
   `infra/theme` に `colorSchemeAtom`（light/dark/system）+ storage。`ThemeSettingsModal` にトグル追加。
   `layout.tsx` の `<head>` に pre-paint inline script（localStorage + `matchMedia` を読み、
   paint 前に `<html>` へ `data-theme-mode` を付与）+ `<html suppressHydrationWarning>`。
4. **3d. `design.ts` の前景調整**（`feature/dark-inline-foreground`）
   `generateColorConfig(base, isDark)` で前景 / neutral のコントラストのみ dark 調整。
   ブランド色相は保持。`themeConfigAtom` の入力に colorScheme を足し inline のまま追従させる。
5. **3e-0. 対応表の作成**（実装前の設計ステップ）
   143 箇所 → semantic トークンの**対応表を先に作る**。実測した内訳は gray が 4 段階
   （`text-gray-400` 14 / `-500` 26 / `-600` 8 / `-700` 4）ある一方、受け皿の text 系トークンは
   `--color-text` / `-primary` / `-secondary` / `-muted` / `-inverse` の 5 個。
   **どの段階をどのトークンへ畳むか**と、**畳んだ結果 light の見た目が変わることを許容するか**を
   ここで決める。あわせて `text-blue-600`（8 箇所・リンク色）を `--color-primary` に寄せて
   **6 色テーマに追従させる**のか、固定色のまま semantic 化するのかを分類する。
6. **3e. ハードコード色 143 箇所の repoint**（`feature/dark-repoint-*`・**画面群ごとに複数 PR**）
   3e-0 の対応表に従って置換する。`dark:` バリアントは semantic トークンが
   存在しない一点ものに限定する。PR は admin 系 / 回答系 / 認証系 / 共通コンポーネントで分割。

## 検証

- **3a**: `<html data-theme-mode="dark">` を手で付けた状態で、**semantic トークンを参照している
  部品**（面・境界・テキスト）が反転する。`design.ts` の inline style 由来の色（ブランド色の塗り）は
  **3d まで据え置き**で反転しないのが正しい挙動 — この境界を目視で切り分けて確認する。
  light（属性なし）の見た目は**変わらない**
- **3b**: dark で地色が反転し、light で従来と同一
- **3c**: light / dark / system の切替が永続する（リロード後も保持）。
  **リロード時に明色フラッシュが出ない**（DevTools の低速 CPU で確認）。
  system 選択時に OS 設定の変更へ追従する
- **3d**: ブランド色（6 色テーマ）の色相が dark でも保たれ、前景のコントラストだけ改善する
- **3e**: 各 PR で light の差分を [VRT](2026-08-14-0020-vrt.md) の compare にかけ、
  **出た差分を 1 件ずつ「3e-0 で意図した統合」と「事故」に仕分ける**。
  差分ゼロは基準にしない（gray 4 段階を畳む以上、意図した変化は必ず出る）。
  意図した統合は対応表と突き合わせて承認し、対応表に無い差分は事故として直す
- 全画面をダークで目視（コントラスト・破綻なし）
- 共通: `pnpm turbo run typecheck lint build test`

## リスク

| リスク | 対応 |
|---|---|
| **VRT の light baseline が間に合わない**（stg MinIO 待ち） | 3e を画面群ごとの小さい PR に割り、各 PR で before/after を手動比較する。VRT が使えるようになった時点で機械化へ切り替える |
| 色が 2 系統（A: `design.ts` inline / B: semantic トークン）+ 143 箇所の三層 | 反転境界を B に固定。A は前景のみ調整し、ブランド色相には触らない |
| **FOUC / hydration flash**（Jotai は hydration 後に確定） | pre-paint inline script + `suppressHydrationWarning`（3c）。低速 CPU での目視を検証項目にする |
| **背景テーマ 9 軸 × dark の組合せが 18 通り**になり目視が膨らむ | dark と背景軸は**直交**（背景の選択は保持し、前景/面だけ反転）。目視は代表 3 背景 × dark に絞り、残りは semantic トークン経由なので同挙動とみなす |
| 143 箇所の一括置換は差分が巨大でレビュー不能 | 画面群ごとに複数 PR。1 PR あたりの変更ファイルを 10 前後に抑える |
| semantic トークンに対応物が無い色があり `dark:` が乱発される | 対応物が無い場合は**まずトークンを足す**ことを検討し、`dark:` は一点ものに限定する。乱発を避けるため各 PR で `dark:` の追加数を記録する |
| `prefers-color-scheme` のデッドコードが生きて二重制御になる | 3a で `globals.css` の当該ブロックを**削除する**（二重制御の芽を残さない） |
| **対応表が肥大し 3e-0 で止まる**（143 箇所を全部分類しきれない） | 出現数の多い上位（gray 4 段階 52 箇所 / `text-red-600` 23 / `border-gray-*` 13 / `bg-white` 8）で 7 割弱を占める。まず上位を決め、残りは PR ごとに追記する運用にする |

## a11y の目標（提案）

今回は **WCAG AA 全面準拠を目標にしない**。理由は 143 箇所の置換と AA 監査を同時に走らせると
PR が肥大し、どちらも中途半端になるため。本 Plan では次に留める:

- 主要導線（ログイン / ダッシュボード / 回答フロー / admin 一覧）のテキストコントラストを
  dark で目視確認する
- 明らかに読めない箇所はその場で直す
- AA の網羅監査は別 Plan に切り出す

## 判断ログ

| 日付 | 判断 | 理由 |
|---|---|---|
| 2026-08-14 | dark と背景テーマ 9 軸は **直交**（背景はそのまま・前景/面だけ反転）（笹木さん選択） | ユーザーの背景選択を dark が上書きしない。組合せは 18 通りになるが、反転が semantic トークン層で一元化されるため目視は代表 3 背景に絞れる |
| 2026-08-14 | FOUC 対策として **pre-paint inline script を入れる**（提案） | Jotai は hydration 後に確定するため、対策なしではリロードのたびに明色フラッシュが出る。ダークを選ぶ動機（眩しさ回避）を最も損なう症状のため許容しない |
| 2026-08-14 | a11y は **目視 + 主要導線のみ**。WCAG AA 全面準拠は別 Plan（提案） | 143 箇所の置換と AA 監査の同時進行は PR を肥大させ、どちらも中途半端になる |
| 2026-08-14 | 現状コンテキストを再実測（ハードコード色 114 → **143 箇所 / 33 ファイル**、semantic トークン **106 個**） | 親 Plan の調査は 2026-07-05 時点。以後 #97 / #103 / #104 / #110 が入り実態が動いていた |
| 2026-08-14 | 3a を**単独 PR**として先に landing させ、semantic トークン経由の部品が反転することを検証してから 143 箇所へ進む | 反転境界の設計が誤っていた場合、143 箇所を触った後では手戻りが大きい |
| 2026-08-14 | 3a の検証基準を「catalog が反転する」→ **「semantic トークン参照部分が反転し、inline style 由来は 3d まで据え置き」** に限定（計画レビュー HIGH） | catalog の色は System A の inline style でも塗られており、トークン上書きだけでは反転しない。当初の基準は達成不能で、「設計が間違っている」と誤判断する恐れがあった |
| 2026-08-14 | **「light の見た目が不変」を検証基準から外し、「差分を意図/事故に仕分ける」に変更**。前段に対応表作成（3e-0）を追加（計画レビュー HIGH） | 実測すると gray は 4 段階使われている一方 text 系トークンは 5 個で、畳めば light は必ず変わる。「差分ゼロ」を基準にすると正当な変更まで事故扱いになり運用が破綻する |
| 2026-08-14 | `prefers-color-scheme` のデッドコードは 3a で**削除**する | 「削除 or 放置」の両論併記だと二重制御の芽が残る |
| 2026-08-14 | FOUC 対策は **pre-paint inline script を入れる**、a11y は **目視 + 主要導線のみ**（AA 全面は別 Plan）で確定 | 計画レビューで提案どおり確定。未確定事項から判断ログへ移動 |
| 2026-08-14 | **3a / 3b / 3c を 1 本の PR にまとめる**（笹木さん選択）。Plan の「3a は単独 PR」方針から外れる | 属性を手で付けないと確認できない状態で止めるより、切替 UI まで通して検証したいという判断。3a の設計妥当性は「semantic トークン参照部分だけが反転する」ことをビルド出力と反転対象ファイル数で確認して担保する |
| 2026-08-14 | `body` の文字色が **`#000000` → `#111827`** に変わることを受け入れる（笹木さん選択） | 元コードは `var(--color-black, #111827)` で、フォールバックが示す意図は `#111827` だった。`--color-black` が定義済みのため実際には純黒で描画されていた。VRT baseline を取る前なので今のうちに吸収できる |
| 2026-08-14 | `prefers-color-scheme` ブロックは **Storybook では生きていた**（Plan の「デッドコード」は誤り）。削除して `data-theme-mode` 一本に統一 | `packages/ui/core/styles/globals.css` は `.storybook/preview.tsx` が import しており、OS がダークの閲覧者には効いていた。削除により Storybook が常に light になり、VRT の撮影が決定的になる（副次的な利点） |
| 2026-08-14 | pre-paint script を **React 非依存の `colorSchemeScript.ts` へ分離**し、専用の export path を切る | `layout.tsx`（Server Component）が hooks を含むモジュールを import するとビルドが落ちる。定数だけを分離することで Server / Client 双方から安全に参照できる |
| 2026-08-14 | dark の `--color-bg-subtle` / `--color-border-light` を**面色と別値にする**（コードレビュー HIGH 2 件） | 当初どちらも `#1e293b` にしたが、DataTable の面色 `--color-surface` と同値だったため、dark で zebra と行区切りが消えていた。light の序列（面 < 縞 / 罫線）を dark でも保つ値へ修正 |
| 2026-08-14 | pre-paint script に **vitest を追加**（コードレビュー MEDIUM） | `<head>` の生文字列で型検査もリンタも通らず、壊れても「一瞬フラッシュする」だけで目視では気付けない。出荷される文字列そのものを eval して 8 ケースを固定した |

## 未確定事項

- 3e-0 の対応表で「gray 4 段階をどこまで畳むか」と「畳んだ結果の light の変化を許容するか」。
  上位の出現数（gray 52 / red 23 / border 13 / bg-white 8）を見ながら実装着手時に決める。
- 3e の PR 分割単位（画面群の切り方）は、3a〜3d 完了後に実際の差分規模を見て確定する。

## 残課題

- WCAG AA の網羅監査（別 Plan）
- ダーク撮影の VRT 追加（4-dark。[VRT Plan](2026-08-14-0020-vrt.md) 完了後）

## ステータス

- [x] 計画確定（計画レビュー APPROVE・FOUC / a11y を確定）
- [x] 3a. semantic トークンの dark 上書き（16 トークン + `@custom-variant dark`・2026-08-14）
- [x] 3b. `body` 地色の semantic 化（app / Storybook 両方・2026-08-14）
- [x] 3c. colorScheme 軸 + 切替 UI + FOUC 対策（2026-08-14）
- [ ] 3d. `design.ts` の前景/neutral 調整
- [ ] 3e. ハードコード色 143 箇所の repoint（複数 PR）
- [ ] マージ後検証
  - [ ] light / dark / system の切替と永続
  - [ ] リロード時に明色フラッシュが出ない（低速 CPU で確認）
  - [ ] 代表 3 背景 × dark で破綻なし
  - [ ] 主要導線のテキストコントラストを dark で目視確認
