# 認証エンドポイント用の一次レートリミット zone（D-4: nginx 一次 + アプリ層多層）。
# キーは $binary_remote_addr = edge(nginx) が観測した実 IP。XFF 詐称の影響を受けない。
# rate はアプリ層 loginPerIp(10/min) と整合。burst は server 側 location で付与する。
limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;
limit_req_status 429;
